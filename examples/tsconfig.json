{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "rootDir": "..",
    "outDir": "../dist",                                   /* Specify an output folder for all emitted files. */
  },
  "exclude": [
    "../node_modules",
    "../dist"
  ]
}
